replicaCount: 1

image:
  repository: mongo
  tag: latest
  pullPolicy: IfNotPresent

service:
  name: user-mongo-service
  type: ClusterIP
  port: 27017
  selector: user-mongo

storage:
  size: 1Gi
  path: /mnt/data/mongo
  accessMode: ReadWriteOnce
